<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.Views.KategoriaView"
             x:Name="This">
	<StackLayout Margin="10,5">
		<Frame Padding="15" BackgroundColor="LightBlue" CornerRadius="8">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="Auto"/>
				</Grid.ColumnDefinitions>

				<Label Text="{Binding NazwaKategorii}" FontSize="20" FontAttributes="Bold"
				   VerticalOptions="Center" Grid.Column="0"/>

				<Button Text="{Binding TekstPrzycisku}" Clicked="OnRozwinClicked"
				   WidthRequest="50" HeightRequest="40" Grid.Column="1"/>
			</Grid>
		</Frame>

		<StackLayout x:Name="ListaProduktow" IsVisible="{Binding CzyRozwinieta}" Padding="10,0,0,0">
		</StackLayout>

		<Button Text="+ Dodaj produkt" Clicked="OnDodajProduktClicked"
		   IsVisible="{Binding CzyRozwinieta}" Margin="10,5"/>
	</StackLayout>
</ContentView>
